<!DOCTYPE html>
<html>
      
  <%- include('./partials/head.ejs') %>

  <body>
    <%- include('./partials/nav.ejs') %>
    <div class="index-first">
       
    </div>

    <div class="index-second">
        <h2>Welcome to the Ultimate Drake Fan Website</h2>

        <p>Welcome to the ultimate destination for fans of the one and only Drake! Whether you’ve been with him since the So Far Gone days, vibe to the unforgettable hits of Take Care, or are just discovering his music, this site is dedicated to celebrating the life, music, and legacy of the 6 God.

            Drake, born Aubrey Drake Graham, has become one of the most influential artists of our generation. From his humble beginnings as an actor on Degrassi: The Next Generation to dominating the charts with iconic albums like Nothing Was the Same, Views, and Certified Lover Boy, Drake has consistently redefined the music industry.
            
            Here, you’ll find everything you need to stay connected to all things Drake:</p>

           
             
    </div>
    <div class="index-neuro">
        <div class="index-g">
            <h2>Here, you’ll find everything you need to stay connected to all things Drake:</h2>

        <p>🎵 Music & Lyrics – Explore his groundbreaking albums, chart-topping singles, and the lyrics that resonate with millions of fans worldwide.</p>
        
        <p>📀 Discography(Albums & Mixtapes) – Relive the magic of every release, from his earliest mixtapes like Room for Improvement to his latest hits.</p>

        <p>🎤 Tour Updates – Stay in the loop about upcoming concerts and tour dates, so you never miss the chance to see Drake perform live.</p>

        <p>🌍 Drake’s Impact – Learn about his influence on music, pop culture, and the city of Toronto, which he continues to proudly represent.</p>

        <p>Whether you're here to reminisce on classic tracks like “Marvins Room” and “Hotline Bling,” or celebrate his latest collaborations, this site is for you. Drake’s music has a unique way of making us feel, and this site is a space to connect with that magic.

            So, grab a drink, press play on your favorite Drake song, and explore. Let’s honor the artistry, talent, and legacy of one of the greatest to ever do it.
            
            Started from the bottom, now we’re here!</p>
        </div>

        <div class="index-g">
            <img class="index-neuropix1"     src="https://upload.wikimedia.org/wikipedia/commons/2/28/Drake_July_2016.jpg" 
            alt="Drake performing in July 2016" 
            >
        </div>
       

    </div>

    <div class="kelz-images">
        <div class="kelz-home">
            <img class="kelz-2home" src="/img/f1.jpeg">
 
         </div>
         <div class="kelz-home">
             <img class="kelz-2home" src="/img/f2.jpeg">
         </div>
         <div class="kelz-home">
             <img class="kelz-2home" src="/img/f3.jpeg">
         </div>
         <div class="kelz-home">
             <img class="kelz-2home" src="/img/f4.jpeg">
         </div>
        </div>


         <!-- NEW: Latest Discography Posts Section -->
    <!-- Latest Discography Posts Section -->
    <div class="container">
        <section class="latest-posts">
            <a href="/discography" style="text-decoration: none;">
                <h2>Discography</h2>
              </a>
              
          <% if (discographyPosts && discographyPosts.length) { %>
            <div class="posts-grid">
              <% discographyPosts.forEach(post => { %>
                <div class="card">
                  <a href="/discography/<%= post.slug %>" class="card-link">
                    <div class="card-body">
                      <h4 class="class-title"><%= post.title %></h4>
                      <div class="sub-title"><%= post.createdAt.toLocaleDateString() %></div>
                      <% if (post.imagePath) { %>
                        <div class="image-container">
                          <img src="/discography/image/<%= post.imagePath %>" 
                               alt="<%= post.title %>" 
                               class="article-image"
                               onerror="this.onerror=null; this.src='/images/placeholder.jpg';">
                        </div>
                      <% } %>
                      <div class="class-text"><%= post.description %></div>
                    </div>
                  </a>
                  <% if (user && user.isAdmin) { %>
                    <div class="btn-flex">
                      <a href="/discography/edit/<%= post.id %>" class="Edit">Edit</a>
                      <form action="/discography/<%= post.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
                        <button type="submit" class="btn-danger">Delete</button>
                      </form>
                    </div>
                  <% } %>
                </div>
              <% }); %>
            </div>
          <% } else { %>
            <p>No discography posts available yet.</p>
          <% } %>
        </section>
      </div>
  
      <!-- Latest Updates Section -->
      <div class="container">
        <section class="latest-posts">
         <a href="/updates" style="text-decoration: none;">
            <h2> Updates</h2>
         </a>
          <% if (updatesPosts && updatesPosts.length) { %>
            <div class="posts-grid">
              <% updatesPosts.forEach(post => { %>
                <div class="card">
                  <a href="/updates/<%= post.slug %>" class="card-link">
                    <div class="card-body">
                      <h4 class="class-title"><%= post.title %></h4>
                      <div class="sub-title"><%= post.createdAt.toLocaleDateString() %></div>
                      <% if (post.imagePath) { %>
                        <div class="image-container">
                          <img src="/updates/image/<%= post.imagePath %>" 
                               alt="<%= post.title %>" 
                               class="article-image"
                               onerror="this.onerror=null; this.src='/images/placeholder.jpg';">
                        </div>
                      <% } %>
                      <div class="class-text"><%= post.description %></div>
                    </div>
                  </a>
                  <% if (user && user.isAdmin) { %>
                    <div class="btn-flex">
                      <a href="/updates/edit/<%= post.id %>" class="Edit">Edit</a>
                      <form action="/updates/<%= post.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
                        <button type="submit" class="btn-danger">Delete</button>
                      </form>
                    </div>
                  <% } %>
                </div>
              <% }); %>
            </div>
          <% } else { %>
            <p>No updates available yet.</p>
          <% } %>
        </section>
      </div>
  
      <!-- Latest Records Section -->
      <div class="container">
        <section class="latest-posts">
          <a href="/records" style="text-decoration: none;"><h2> Records </h2></a>
          <% if (recordsPosts && recordsPosts.length) { %>
            <div class="posts-grid">
              <% recordsPosts.forEach(post => { %>
                <div class="card">
                  <a href="/records/<%= post.slug %>" class="card-link">
                    <div class="card-body">
                      <h4 class="class-title"><%= post.title %></h4>
                      <div class="sub-title"><%= post.createdAt.toLocaleDateString() %></div>
                      <% if (post.imagePath) { %>
                        <div class="image-container">
                          <img src="/records/image/<%= post.imagePath %>" 
                               alt="<%= post.title %>" 
                               class="article-image"
                               onerror="this.onerror=null; this.src='/images/placeholder.jpg';">
                        </div>
                      <% } %>
                      <div class="class-text"><%= post.description %></div>
                    </div>
                  </a>
                  <% if (user && user.isAdmin) { %>
                    <div class="btn-flex">
                      <a href="/records/edit/<%= post.id %>" class="Edit">Edit</a>
                      <form action="/records/<%= post.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
                        <button type="submit" class="btn-danger">Delete</button>
                      </form>
                    </div>
                  <% } %>
                </div>
              <% }); %>
            </div>
          <% } else { %>
            <p>No records posts available yet.</p>
          <% } %>
        </section>
      </div>
  

    <%- include('./partials/footer.ejs') %>
    <script>
        function toggleMobileMenu(menu) {
        var isOpen = menu.classList.toggle('open');
        if (!isOpen) {
            closeDropdowns(); // Close dropdowns when menu is closed
        }
      }
      
      // Function to toggle dropdown
      function toggleDropdown(event) {
          event.stopPropagation(); // Prevent propagation of the click event
          var parentListItem = event.target.parentElement;
          parentListItem.classList.toggle('open');
      }
      
      // Function to close all dropdowns
      function closeDropdowns() {
          var dropdowns = document.querySelectorAll('.has-dropdown');
          dropdowns.forEach(function(dropdown) {
              dropdown.classList.remove('open');
          });
      }
      
      // Close dropdowns when clicking outside of them
      document.addEventListener('click', function(event) {
          var dropdowns = document.querySelectorAll('.has-dropdown');
          dropdowns.forEach(function(dropdown) {
              if (!dropdown.contains(event.target)) {
                  dropdown.classList.remove('open');
              }
          });
      });
      
      // Close dropdowns when the window is resized (for responsiveness)
      window.addEventListener('resize', function() {
          closeDropdowns();
      });
      
      // Close dropdowns when the mobile menu is closed
      document.getElementById('ham-icon').addEventListener('click', function(event) {
          if (!event.currentTarget.classList.contains('open')) {
              closeDropdowns();
          }
      });
      
      document.addEventListener("DOMContentLoaded", function () {
  const items = document.querySelectorAll(".kelz-home");

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add("visible");
      } else {
        entry.target.classList.remove("visible");
      }
    });
  }, { threshold: 0.1 });

  items.forEach(item => observer.observe(item));
});
    </script>
    
  </body>
</html>